(ns kigen.holonomy
  (:use [kigen.orbit :as o]))

;; elts - set of elements
;; rel? - a partial order relation pred on elts
(defn hasse-diagram
  [elts rel?]
  )

;; set
;; hd Hasse-diagram
(defn tile-chain
  [set hd]
  )
